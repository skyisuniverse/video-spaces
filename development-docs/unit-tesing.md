# Unit Testing

To run unit tests and open an html report (with **coverage** and **watch mode**)

```shell
npm run test:ui
```

To run unit tests with report in console (without watch mode)

```shell
npm run test
```
